### Uncomment following line and replace "password" with actual password for local MySQL database server.
#in.db.password			= password

in.db.hostname			= localhost
in.db.username			= root
in.db.queries.filename		= setup/BB.SQL_queries.default.txt

# Includes (should be at the end so any values defined in current file will override those in the included files).
#include				= 